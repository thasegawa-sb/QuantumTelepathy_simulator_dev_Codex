# Decisions

| Date | Decision | Rationale |
|---|---|---|
| 2026-09-01 | Initialize a fresh Git repository in the workspace. | User confirmed no Git repository existed and requested `git init`. |
| 2026-09-01 | Implement the smallest scientific core before Li operational layers. | CHSH and deterministic classical baselines are mandatory gates for both Ding-Jiang and Li et al. |
| 2026-09-01 | Keep Ding-Jiang and Li et al. wrappers separate while sharing the XOR core. | Prevents Li operational semantics from silently changing Ding reproduction results. |
| 2026-09-01 | Implement Li combined infidelity using the exact expression from Eq. 30. | The small-error approximation is not acceptable as the reference model. |
| 2026-09-01 | Treat microscopic cQED reproduction as deferred. | Li Table III system-level formulas are sufficient for the mandatory next hardware gate; appendix-level microscopic reproduction may require more data. |
| 2026-09-01 | Report the Ding Figure 3 computation gate as PASS but the paper-plot reproduction as PARTIAL. | Exact analytical checks pass, while author source data for nonzero beta is unavailable. |
| 2026-09-01 | Implement photon loss both as an Eq. A.11 probability mixture and an Eq. A.12 Bell operator. | The two paths provide an independent numerical cross-check of the loss semantics. |
| 2026-09-01 | Use a deterministic angle grid, Powell refinement, and all 16 fallback strategies. | Matches Appendix B.3 without stochastic optimizer variance. |
| 2026-09-01 | Represent the Ding-Jiang Type II calculation as a paper-specific M1 model with decomposed timing and two-arm transmission terms. | Preserves the corrected v3 formula and prevents accidental reuse as Li's occupancy-aware M2 model. |
| 2026-09-01 | Validate Type II against both exact formula-derived Decimal values and separately rounded publication values. | The published `230 us`, `0.0248`, and `106 Hz` have different stated precision from the underlying formulas. |
| 2026-09-01 | Implement Ding depolarizing `nu` in a paper-specific module rather than reuse Li fidelity code. | Ding Eq. 4.2 is a state-depolarizing qubit behavior; Li `epsilon` combines state and measurement infidelity under a different model. |
| 2026-09-01 | Preserve signed noisy gaps in Figure 8 data and clip only visualized surfaces at zero. | Negative values establish failure of theoretical advantage and must remain available for falsification. |
| 2026-09-02 | Bound Ding Figure 5 thresholds from both sides using explicit qubit strategies and an independent NPA `Q1+AB` relaxation. | A production strategy optimizer cannot serve as its own global-optimality oracle. |
| 2026-09-02 | Keep Figure 5 reproduction `PARTIAL` after scalar and cross-section gates pass. | Author pointwise data and the cited unpublished modified-NPA implementation are unavailable; the full 101x101 surface was not run. |
| 2026-09-02 | Use the NPA result for the exact CHSH `2/3` endpoint gate and report explicit-strategy endpoint excess separately. | Near `2/3`, positive utility gain approaches machine precision and an explicit optimizer's detection threshold is resolution-sensitive. |
| 2026-09-02 | Label the NPA objective allowance as a solver error margin, not a certification margin. | Adding a numerical margin to a solver result does not create a formal SDP certificate. |
| 2026-09-02 | Implement Li Eq. 26 and Eq. 28-29 as a density-matrix trace oracle in addition to the closed-form Eq. 30 path. | The exact combined-infidelity formula must be checked against an independently evaluated physical state and observables. |
| 2026-09-02 | Interpret Li Figure 2(b)'s caption relation with `P00=1-2P11` and retain the Eq. 24/35-consistent negative `(1,1)` matrix sign. | This preserves probability normalization and the canonical CHSH limit despite the positive sign printed in the displayed Eq. 25 matrix. |
| 2026-09-02 | Mark Li Figure 2 computation gates `PASS` but paper-level reproduction `PARTIAL`. | No author pointwise data are available for direct numerical comparison; analytical and independent-code oracles do pass. |
| 2026-09-02 | Use SciPy's binomial survival function with vectorized exhaustive chunks for Li `n_req`. | It preserves the exact discrete tail and strict threshold while avoiding direct-power underflow and normal-approximation error. |
| 2026-09-02 | Reuse the previous `n_req` only as a proven lower bound along nonincreasing `omega_Q` sequences. | At every fixed round count, lowering the quantum win probability cannot lower the classical tail p-value, so no earlier solution can appear. |
| 2026-09-02 | Distinguish no finite certification from an exhausted numerical search. | `omega_Q<=omega_C` is a scientific no-solution condition; reaching `max_rounds` is a configurable computational limit. |
| 2026-09-02 | Mark Li Figure 3 computation gates `PASS` but paper-level reproduction `PARTIAL`. | Exact equations and independent Decimal checks pass, while author pointwise curve data are unavailable. |
| 2026-09-02 | Treat the Table II status as prospective system-level feasibility, not an observed experiment result. | Li Eq. 40 certifies the expected quantum win count; observed wins require a separate experimental p-value evaluation. |
| 2026-09-02 | Require the LCTC regime and every theoretical, fidelity, finite-statistics, rate, and decision status for overall PASS. | This prevents a positive ideal gap or any single hardware criterion from being reported as operational quantum advantage. |
| 2026-09-02 | Implement Li M2 in a new platform-independent hardware package while leaving Ding M1 unchanged. | M2 occupancy, decoherence, and channel multiplexing must not silently alter the validated Ding traversal-rate model. |
| 2026-09-02 | Report Eq. 49 outputs outside the valid state/combined-infidelity domain instead of clipping them. | Clipping would hide hardware regimes where the analytical error model is no longer physically usable. |
| 2026-09-02 | Limit Phase 16 final narrative artifacts to two Japanese Word documents and one English LaTeX paper. | The user specified a Japanese simulator manual, Japanese report, and English research paper as the three required files. |
| 2026-09-02 | Preserve both attenuation/latency laws and the rounded Table III operating values in the 50 km benchmark. | The paper explicitly substitutes `eta_att=0.06` and `tau_link=240 us`, while its listed attenuation coefficient and group velocity produce different unrounded values. |
| 2026-09-02 | Mark Table III paper reproduction `PARTIAL` while passing the equation and operational gates. | Direct evaluation reproduces `R_HEG` and `epsilon`, but `R0`, `p_ent`, `tau_occ`, and `p_false` lie outside their displayed-value half-unit intervals; forcing intermediate rounding would hide the discrepancies. |
| 2026-09-03 | Implement Phase 10 as a work-conserving, single-channel finite-memory event scheduler with independent PCG64 herald sampling. | This is an implementation independent of Eq. 48's closed-form minimum while preserving the exact Table III system-level assumptions. |
| 2026-09-03 | Validate Monte Carlo throughput with rate error, t interval, exact binomial interval, standardized residual, sample variance, and convergence gates. | A single mean comparison cannot distinguish scheduler bias from expected finite-sample fluctuation. |
| 2026-09-03 | Implement Li Eq. 20 directly with log-space interpolated binomial tails for fractional utility certification. | Applying the exact win/loss binomial tail to fractional scores would use the wrong sampling model; a looser unrelated approximation would not reproduce the paper. |
| 2026-09-03 | Report the Ding-to-Li game change separately from physical error loss in the HFT waterfall. | Changing input correlations or utility asymmetry changes the game itself and cannot be interpreted as hardware degradation. |
| 2026-09-03 | Derive Phase 11 hardware inputs from the validated Table III artifact and record its digest. | This keeps the final feasibility result traceable to lower-level device/network parameters and detects stale upstream results. |
| 2026-09-03 | Put generic multiparty XOR primitives in a separate package and keep Li's majority/GHZ model in `li2026`. | The reusable local-strategy and phase machinery should not hardcode one paper utility, while paper-specific noise semantics must remain traceable. |
| 2026-09-03 | Use a symmetric stationary polynomial for the Figure 7(b) production grid and unrestricted three-phase differential evolution only as an independent oracle. | This gives deterministic, fast full-grid evaluation without using the same numerical implementation as its validation path. |
| 2026-09-03 | Apply exact binomial certification to Li Eq. 65 and retain Eq. 20 for score-interpreted games. | The paper explicitly assigns Eq. 16 to the multiparty utility; selecting statistics by numerical range alone would lose the stated operational interpretation. |
| 2026-09-03 | Leave Figure 7(c-e) CAPS dynamics outside the validated multiparty game module. | Bipartite TPI and multipartite single-photon CAPS have different physical rate/fidelity models and cannot share the Table III formula. |
| 2026-09-03 | Use exhaustive finite-grid enumeration for Phase 13 before introducing stochastic or Bayesian optimizers. | It gives an exact search-space infeasibility result, deterministic Pareto membership, and no convergence ambiguity at the current grid size. |
| 2026-09-03 | Report Pareto-optimal designs alongside an equal-weight normalized recommendation. | No defensible empirical hardware-development cost model is available, so one scalar optimum alone would overstate the result. |
| 2026-09-03 | Remove rounded Table III link overrides during distance optimization. | Keeping the 50 km overrides while changing distance would silently decouple attenuation and communication latency from distance. |
| 2026-09-03 | Reevaluate every selected recommendation through the validated Phase 11 operational API. | The optimizer's cached screening path must not serve as its own oracle for `n_req`, `R_req`, or PASS/FAIL status. |
| 2026-09-03 | Hoist only scenario-invariant game values, fidelity threshold, and score range out of the Phase 13 candidate loop. | These quantities cannot depend on hardware design; computing them once removes 43,767 repeated game optimizations per complete run without changing candidate semantics. |
| 2026-09-03 | Preserve an exhaustive contiguous finite-statistics scan while growing blocks from 256 to the configured chunk size. | This reduces over-computation for small `n_req` without assuming monotonicity across the ceiling-induced p-value sawtooth or skipping any candidate round. |
| 2026-09-03 | Skip the second Eq. 20 binomial tail only when the interpolation fraction is exactly zero. | In that case its exponent is zero and it cannot affect the bound; fractional-score cases still evaluate both tails. |
| 2026-09-03 | Keep the Figure 5 Q1+AB threshold workflow sequential. | The representative calculation is stable at about 0.23 seconds; concurrent conic solves would risk solver oversubscription and nondeterminism while the deferred full workflow is dominated by the separate explicit-strategy search. |
| 2026-09-03 | Benchmark major targets in isolated subprocesses and gate scientific output independently of timing. | Process-level peak RSS and wall time need workload isolation, while platform variation must never be mistaken for a scientific regression. |
